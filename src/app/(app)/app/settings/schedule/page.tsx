import { PagePlaceholder } from "@/components/layout/page-placeholder";

export default function ScheduleSettingsPage() {
  return (
    <PagePlaceholder
      title="Work schedule"
      description="Configure weekday work sessions or scheduled days off."
      phaseNote="Compressed-week schedule builder arrives in Phase 3."
    />
  );
}
