import { PagePlaceholder } from "@/components/layout/page-placeholder";

export default function TemplatesSettingsPage() {
  return (
    <PagePlaceholder
      title="Templates"
      description="Verify active DOCX and XLSX runtime template versions."
      phaseNote="Template availability checks arrive with Phase 3 and export phases."
    />
  );
}
