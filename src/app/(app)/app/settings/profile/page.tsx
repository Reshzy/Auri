import { PagePlaceholder } from "@/components/layout/page-placeholder";

export default function ProfileSettingsPage() {
  return (
    <PagePlaceholder
      title="Profile & office"
      description="Employee name, municipality, office, and department."
      phaseNote="Editable profile settings arrive in Phase 3."
    />
  );
}
